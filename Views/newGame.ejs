<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Game</title>
</head>
<body>
    <div>
        <form action="/newGame" method="POST" name="newGame">
            <div>
                <label id="title">Title:</label>
                <input type="text" name="title" value="<%=data?.title || ''%>">
                <%errors?.filter(err => err.path == 'title').forEach(err => {%>
                    <p><%=err.msg%></p>
                <%})%>
            </div>

            <div>
                <label id="image">Image:</label>
                <input type="text" name="image" value="<%=data?.image || ''%>">
                <%errors?.filter(err => err.path == 'image').forEach(err => {%>
                    <p><%=err.msg%></p>
                <%})%>
            </div>

            <div>
                <label id="price">Price:</label>
                <input type="number" step="0.01" name="price" value="<%=data?.price || ''%>">
                <%errors?.filter(err => err.path == 'price').forEach(err => {%>
                    <p><%=err.msg%></p>
                <%})%>
            </div>

            <div>
                <label id="reviews">Reviews:</label>
                <input type="number" step="0.01" name="reviews" value="<%=data?.reviews || ''%>">
                <%errors?.filter(err => err.path == 'reviews').forEach(err => {%>
                    <p><%=err.msg%></p>
                <%})%>
            </div>

            <div>
                <label id="genre">Genre:</label>
                <input type="text" name="genre" value="<%=data?.genre || ''%>">
                <%errors?.filter(err => err.path == 'genre').forEach(err => {%>
                    <p><%=err.msg%></p>
                <%})%>
            </div>
            <button type="submit">Add</button>
        </form>
    </div>
</body>
</html>